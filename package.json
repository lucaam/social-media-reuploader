{
  "name": "telegram-downloader-dev",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0"
  },
  "scripts": {
    "commitlint": "commitlint",
    "commitlint:range": "commitlint --from=HEAD~20 --to=HEAD"
  }
}
